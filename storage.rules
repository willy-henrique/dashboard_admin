rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir acesso público total para desenvolvimento
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
