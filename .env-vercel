# ===========================================
# CONFIGURAÇÃO VERCEL - PAINEL ADMINISTRATIVO
# ===========================================
# Este arquivo contém todas as variáveis necessárias para integração total do Firebase
# e funcionamento completo do painel administrativo no Vercel

# ==== CORE APPLICATION ====
NODE_ENV=production
APP_ENV=production
PORT=3000

# ==== FIREBASE CONFIGURATION ====
# Obtenha essas credenciais no Console do Firebase (https://console.firebase.google.com)
# Projeto > Configurações do projeto > Seus aplicativos > Configuração do SDK

# Chave da API do Firebase (pública)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here

# Domínio de autenticação do Firebase
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# ID do projeto Firebase
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Bucket de armazenamento do Firebase
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# ID do remetente de mensagens
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012

# ID do aplicativo Firebase
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# ID de medição do Google Analytics (opcional)
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ==== GOOGLE MAPS API ====
# Obtenha a chave no Google Cloud Console (https://console.cloud.google.com)
# APIs e serviços > Credenciais > Chaves de API

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ==== FIREBASE ADMIN SDK (SERVER-SIDE) ====
# Para operações administrativas no servidor (opcional)
# Baixe o arquivo JSON do Console do Firebase > Configurações do projeto > Contas de serviço

FIREBASE_ADMIN_PROJECT_ID=your-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ==== SECURITY & AUTHENTICATION ====
# JWT Secret para autenticação (gere uma chave forte)
JWT_SECRET=your_super_secure_jwt_secret_here_min_32_chars
JWT_EXPIRES_IN=24h

# Chave de criptografia para dados sensíveis
ENCRYPTION_KEY=your_encryption_key_here_min_32_chars
ENCRYPTION_ALGORITHM=aes-256-gcm

# ==== CORS & SECURITY ====
CORS_ORIGIN=https://your-domain.vercel.app
RATE_LIMIT_WINDOW=15m
RATE_LIMIT_MAX=100
BCRYPT_ROUNDS=12

# ==== FEATURE FLAGS ====
# Habilite/desabilite funcionalidades específicas
FEATURE_FIREBASE_AUTH=true
FEATURE_FIREBASE_FIRESTORE=true
FEATURE_FIREBASE_ANALYTICS=true
FEATURE_GOOGLE_MAPS=true
FEATURE_REAL_TIME_TRACKING=true
FEATURE_USER_MANAGEMENT=true
FEATURE_ORDER_MANAGEMENT=true
FEATURE_FINANCIAL_REPORTS=true

# ==== LOGGING & MONITORING ====
LOG_LEVEL=info
LOG_FORMAT=json

# Sentry para monitoramento de erros (opcional)
SENTRY_DSN=your_sentry_dsn_here

# ==== WEBHOOKS & INTEGRATIONS ====
# URLs para webhooks externos (se necessário)
WEBHOOK_FIREBASE_URL=https://your-domain.vercel.app/api/webhooks/firebase
WEBHOOK_GOOGLE_MAPS_URL=https://your-domain.vercel.app/api/webhooks/google-maps

# ==== DEVELOPMENT OVERRIDES ====
# Para desenvolvimento local (não usar em produção)
DEV_MOCK_FIREBASE=false
DEV_MOCK_GOOGLE_MAPS=false
DEV_DEBUG_MODE=false

# ==== INSTRUÇÕES DE CONFIGURAÇÃO ====
# 1. Substitua todos os valores "your_*_here" pelas credenciais reais
# 2. Configure as regras de segurança do Firestore no Console do Firebase
# 3. Habilite as APIs necessárias no Google Cloud Console
# 4. Configure os domínios autorizados no Firebase Auth
# 5. Teste todas as funcionalidades após o deploy

# ==== FIREBASE FIRESTORE RULES (EXEMPLO) ====
# rules_version = '2';
# service cloud.firestore {
#   match /databases/{database}/documents {
#     // Permitir leitura/escrita para usuários autenticados
#     match /{document=**} {
#       allow read, write: if request.auth != null;
#     }
#   }
# }
